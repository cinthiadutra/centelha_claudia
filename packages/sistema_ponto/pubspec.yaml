name: sistema_ponto
description: Sistema de ponto para controle de entrada e saída de membros
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  
  # Database
  supabase_flutter: ^2.0.0
  
  # Dependency Injection
  get_it: ^7.6.4
  
  # Date & Time
  intl: ^0.19.0
  
  # CSV/File handling
  csv: ^6.0.0
  file_picker: ^8.0.0+1
  
  # Navigation
  get: ^4.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/programacao_2026.json
